// Eu coloquei a URL separada do código principal, porque, caso a URL do backend altere,
// fica mais prático para fazer a manutenção.

// Função para configurar a URL base
function URLBASE() {
    // Defina aqui a URL base para as requisições
    return 'http://localhost:5000';
}
